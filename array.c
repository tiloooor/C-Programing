#include <stdio.h>

int main() {

  int MyNumbersArray[3] = {1, 2, 3};

  // Print each element in the array
  for(int i = 0; i < 3; i++) {
    printf("Element[%d] = %d\n", i, MyNumbersArray[i]);
  }

}
